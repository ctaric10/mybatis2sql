<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="demo.UserMapper">
    <sql id="columns">id, ${alias}.name, ${alias}.status</sql>

    <select id="findUsers">
        SELECT <include refid="columns"><property name="alias" value="u"/></include>
        FROM users u
        <where>
            <if test="name != null">AND u.name = #{name}</if>
            <choose>
                <when test="status == 1">AND u.status = 1</when>
                <when test="status == 2">AND u.status = 2</when>
                <otherwise>AND u.status IS NOT NULL</otherwise>
            </choose>
            <if test="ids != null and ids.size() > 0">
                AND u.id IN
                <foreach collection="ids" item="id" index="i" open="(" close=")" separator=",">
                    #{id}
                </foreach>
            </if>
        </where>
        ORDER BY ${sortColumn}
    </select>

    <update id="updateUser">
        UPDATE users
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteUser">
        DELETE FROM users
        <trim prefix="WHERE" prefixOverrides="AND|OR">
            AND id = #{id}
        </trim>
    </delete>
</mapper>
